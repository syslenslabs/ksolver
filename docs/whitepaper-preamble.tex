% Preamble for the GPU-scheduler whitepaper PDF build (pandoc -H).
% Provides TikZ for the §0 Kubernetes-objects figure.
\usepackage{tikz}
\usetikzlibrary{calc,positioning,decorations.pathreplacing,fit,arrows.meta}
