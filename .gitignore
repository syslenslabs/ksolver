target/
.claude/
__pycache__/
*.pyc

# Live cluster captures used by local ksolver demo/replay runs. They contain
# operational node, process, and workload metadata and must never be committed.
/demo/*snapshot*/

# Local review/demo scratch (screenshots captured during UI iteration)
/*.png
# MCP browser-automation scratch
/.playwright-mcp/
# Generated whitepaper PDF (source lives in docs/*.md + docs/*.tex)
docs/*.pdf
